<%= form_for "links/@sponsorship" do |f| %>
  <% if @sponsorship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sponsorship.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% flash[:notice] = "There was an error" %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>

  </div>
  <div class="field">
    <%= f.submit :value => "Submit", :class=> "btn btn-inverse btn-large" %>

  </div>
<% end %>